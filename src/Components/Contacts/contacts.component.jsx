import "./contacts.styles.scss";

const Contacts = () => {
  return (
    <section id="contacts" className="contacts">
      <div className="contacts-heading">
        <h1>Contacts</h1>
        <h2>Contact my parents!</h2>
      </div>
    </section>
  );
};

export default Contacts;
